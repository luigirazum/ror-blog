<section class="hero fs-700 flex">
  <%= render partial: 'users/user_hero', locals: { user: @user } %>
</section>
<section class="user-posts fs-400 border-primary">
  <h2 class="ff-headings fw-bold fs-700">All Posts</h2>
  <%= render_all_posts %>
</section>
<section class="actions">
  <%= link_to 'See all users', root_path, class: 'btn btn--primary' %>
</section>
